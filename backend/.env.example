GITHUB_TOKEN=
MISTRAL_API_KEY=
MISTRAL_MODEL=mistral-small-latest

# Optional: comma-separated browser origins (e.g. https://your-app.vercel.app).
# Leave empty to allow any origin (allow_credentials stays false — OK for Next.js server-side proxy).
BACKEND_CORS_ORIGINS=
